# Because of problems with installing tensorflow from the internal package repository,
# we push the .whl with the application to the container and have pip install from there.
#
# This file contains the path to the wheelfile.
#
packages/tensorflow-2.5.0-cp38-cp38-manylinux2010_x86_64.whl