# Because of problems with installing the spacy model from the internal package repository,
# we push the .whl with the application to the container and have pip install from there.

packages/nl_core_news_md-3.1.0-py3-none-any.whl